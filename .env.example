# Supabase connection (fill these values from your Supabase project settings)
SUPABASE_URL="https://YOUR-PROJECT-REF.supabase.co"
# The service role key must ONLY be used on the server. Never expose it to the browser.
SUPABASE_SERVICE_ROLE_KEY="YOUR_SERVICE_ROLE_KEY"
# Optional: anon key if you ever use the client on the browser (not used in this demo)
SUPABASE_ANON_KEY="YOUR_ANON_KEY"

# SMTP example for contact form
# SMTP example for contact form
# Example: SendGrid (recommended for reliability)
# Create a SendGrid API key and set:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=YOUR_SENDGRID_API_KEY
# Optional override for From header (should match a verified sender)
# SMTP_FROM="JunStats <no-reply@yourdomain.com>"
# Destination for contact form messages
# CONTACT_TO=your@personal.email

# Example: Gmail (only if using App Passwords / appropriate account settings)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=yourgmail@gmail.com
# SMTP_PASS=YOUR_APP_PASSWORD

